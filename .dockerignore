.git
.idea
__pycache__
.pytest_cache
.coverage
*.pyc
*.pyo
*.pyd
.env
docker-compose.yml
Dockerfile
README.md
*.log
logs/
poetry.lock
# Depending on your needs, you might want to keep poetry.lock if you build inside the container using it.
# For the current Dockerfile, it is copied separately, so we can ignore it from the main COPY . .
# If you have other large files/directories not needed in the image, add them here.
docs/
migrations/ # If not needed at runtime inside the container
alembic.ini # If not needed at runtime
